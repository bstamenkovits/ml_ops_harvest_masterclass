{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "model_api.main:app",
                "--reload"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}/.venv/bin/python"
            }
        }
    ]
}